# Secrets & credentials — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
service-account*.json
gcp-*.json
credentials*.json
*-key.json

# Evidence caches / snapshots (large, reproducible)
evidence/cache/
evidence/snapshots/
*.snapshot

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node / frontend
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Devpost submission working folder — private copy-paste material, never published
project submission/

# Raw build/process docs — kept locally, not published to the public repo
SPEC.md
CLAUDE.md
PLAN.md
QUESTIONS.md
RESEARCH.md
.claude/

# Local phase evidence is committed - do NOT ignore evidence/phase_*.txt

# scripts/live_run/artifacts/ — run OUTPUT of the accelerated 4-round self-improving
# live run (generated patients, round_results.json, improve_result.json, the
# FilePromotionLedger dir, trajectory.json/.md): reproducible from the scripts, not
# source. Summary numbers get copied into EVALUATION.md by hand, not this directory.
scripts/live_run/artifacts/

